<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cohesion Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=SF+Pro+Display:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    
    <style>
        * {
            font-family: 'SF Pro Display', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        }

        /* Professional Light Background */
        .liquid-glass-bg {
            background: linear-gradient(135deg, 
                #f8fafc 0%, 
                #e2e8f0 25%, 
                #cbd5e1 50%, 
                #94a3b8 75%, 
                #64748b 100%);
            background-size: 400% 400%;
            animation: liquidFlow 20s ease-in-out infinite;
            position: relative;
            overflow-x: hidden;
            min-height: 100vh;
        }

        .liquid-glass-bg::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 20% 30%, rgba(59, 130, 246, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(16, 185, 129, 0.06) 0%, transparent 50%),
                radial-gradient(circle at 50% 20%, rgba(139, 92, 246, 0.05) 0%, transparent 50%);
            pointer-events: none;
        }

        @keyframes liquidFlow {
            0%, 100% { background-position: 0% 50%; }
            25% { background-position: 100% 25%; }
            50% { background-position: 100% 75%; }
            75% { background-position: 0% 75%; }
        }

        /* Professional Glass Cards */
        .liquid-glass-card {
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.6);
            border-radius: 24px;
            box-shadow: 
                0 8px 32px rgba(0, 0, 0, 0.08),
                0 4px 16px rgba(0, 0, 0, 0.04),
                inset 0 1px 0 rgba(255, 255, 255, 0.9);
            transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
            position: relative;
            overflow: hidden;
            width: 100%;
        }

        .liquid-glass-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 1px;
            background: linear-gradient(90deg, 
                transparent 0%, 
                rgba(59, 130, 246, 0.6) 50%, 
                transparent 100%);
        }

        .liquid-glass-card::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, 
                rgba(59, 130, 246, 0.05) 0%, 
                transparent 50%, 
                rgba(16, 185, 129, 0.03) 100%);
            opacity: 0;
            transition: opacity 0.3s ease;
            pointer-events: none;
        }

        .liquid-glass-card:hover {
            transform: translateY(-8px) scale(1.02);
            background: rgba(255, 255, 255, 0.9);
            border: 1px solid rgba(59, 130, 246, 0.3);
            box-shadow: 
                0 20px 40px rgba(59, 130, 246, 0.12),
                0 8px 24px rgba(0, 0, 0, 0.08),
                inset 0 1px 0 rgba(255, 255, 255, 1);
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .liquid-glass-card {
                border-radius: 16px;
                margin-bottom: 1rem;
            }
            
            .liquid-glass-card:hover {
                transform: translateY(-4px) scale(1.01);
            }
        }

        .liquid-glass-card:hover::after {
            opacity: 1;
        }

        /* Welcome Card */
        .welcome-glass {
            background: rgba(255, 255, 255, 0.7);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            border: 1px solid rgba(255, 255, 255, 0.8);
            border-radius: 32px;
            box-shadow: 
                0 12px 40px rgba(0, 0, 0, 0.06),
                0 6px 20px rgba(0, 0, 0, 0.04),
                inset 0 1px 0 rgba(255, 255, 255, 0.9);
            position: relative;
            overflow: hidden;
            margin: 1rem;
        }

        .welcome-glass::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: conic-gradient(from 0deg, 
                transparent 0deg,
                rgba(59, 130, 246, 0.05) 90deg,
                transparent 180deg,
                rgba(16, 185, 129, 0.04) 270deg,
                transparent 360deg);
            animation: welcomeRotate 15s linear infinite;
            pointer-events: none;
        }

        /* Responsive welcome card */
        @media (max-width: 768px) {
            .welcome-glass {
                border-radius: 20px;
                margin: 0.5rem;
            }
        }

        @keyframes welcomeRotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Professional Icon Containers */
        .icon-container {
            background: linear-gradient(135deg, 
                rgba(59, 130, 246, 0.9) 0%, 
                rgba(37, 99, 235, 0.9) 100%);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.5);
            border-radius: 20px;
            box-shadow: 
                0 8px 32px rgba(59, 130, 246, 0.2),
                inset 0 1px 0 rgba(255, 255, 255, 0.6);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .icon-container.green {
            background: linear-gradient(135deg, 
                rgba(16, 185, 129, 0.9) 0%, 
                rgba(5, 150, 105, 0.9) 100%);
            box-shadow: 
                0 8px 32px rgba(16, 185, 129, 0.2),
                inset 0 1px 0 rgba(255, 255, 255, 0.6);
        }

        .icon-container.purple {
            background: linear-gradient(135deg, 
                rgba(139, 92, 246, 0.9) 0%, 
                rgba(124, 58, 237, 0.9) 100%);
            box-shadow: 
                0 8px 32px rgba(139, 92, 246, 0.2),
                inset 0 1px 0 rgba(255, 255, 255, 0.6);
        }

        .icon-container.orange {
            background: linear-gradient(135deg, 
                rgba(245, 158, 11, 0.9) 0%, 
                rgba(217, 119, 6, 0.9) 100%);
            box-shadow: 
                0 8px 32px rgba(245, 158, 11, 0.2),
                inset 0 1px 0 rgba(255, 255, 255, 0.6);
        }

        .icon-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, 
                rgba(255, 255, 255, 0.2) 0%, 
                transparent 50%, 
                rgba(255, 255, 255, 0.1) 100%);
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        /* Responsive icon containers */
        @media (max-width: 768px) {
            .icon-container {
                border-radius: 16px;
                width: 3rem;
                height: 3rem;
            }
        }

        .icon-container:hover::before {
            opacity: 1;
        }

        /* Professional Text */
        .glass-text {
            background: linear-gradient(135deg, 
                rgba(71, 85, 105, 0.95) 0%, 
                rgba(30, 41, 59, 0.9) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-weight: 700;
        }

        .subtitle-text {
            color: rgba(71, 85, 105, 0.8);
            text-shadow: 0 1px 2px rgba(255, 255, 255, 0.5);
        }

        /* Floating Animation */
        .float-animation {
            animation: float 6s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }

        /* Fade in animations */
        .fade-in-up {
            animation: fadeInUp 0.8s ease-out forwards;
            opacity: 0;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Stagger animation delays */
        .delay-1 { animation-delay: 0.1s; }
        .delay-2 { animation-delay: 0.2s; }
        .delay-3 { animation-delay: 0.3s; }
        .delay-4 { animation-delay: 0.4s; }

        /* Professional Button */
        .apple-button {
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(59, 130, 246, 0.3);
            border-radius: 16px;
            color: #475569;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 
                0 4px 16px rgba(59, 130, 246, 0.1),
                inset 0 1px 0 rgba(255, 255, 255, 0.9);
        }

        .apple-button:hover {
            background: rgba(59, 130, 246, 0.1);
            transform: translateY(-2px);
            box-shadow: 
                0 8px 24px rgba(59, 130, 246, 0.2),
                inset 0 1px 0 rgba(255, 255, 255, 1);
        }

        /* Responsive Typography */
        @media (max-width: 768px) {
            .glass-text {
                font-size: 1.5rem;
            }
            
            .subtitle-text {
                font-size: 0.875rem;
            }
        }

        @media (max-width: 480px) {
            .glass-text {
                font-size: 1.25rem;
            }
        }

        /* Smooth Scrolling */
        html {
            scroll-behavior: smooth;
        }

        body {
            overflow-x: hidden;
        }

        /* Container Responsive */
        .responsive-container {
            padding-left: 1rem;
            padding-right: 1rem;
        }

        @media (min-width: 640px) {
            .responsive-container {
                padding-left: 1.5rem;
                padding-right: 1.5rem;
            }
        }

        @media (min-width: 1024px) {
            .responsive-container {
                padding-left: 2rem;
                padding-right: 2rem;
            }
        }
    </style>
</head>
<body class="liquid-glass-bg min-h-screen">

    <!-- Navigation Bar -->
    <nav class="welcome-glass responsive-container">
        <div class="flex justify-between items-center p-4">
            <div class="flex items-center space-x-3 md:space-x-4">
                <div class="w-10 h-10 md:w-12 md:h-12 rounded-2xl bg-gradient-to-r from-blue-500 to-blue-600 p-2 flex items-center justify-center">
                    <img src="/images/cohesion_logo.png" alt="Cohesion" class="w-full h-full object-contain rounded-xl">
                </div>
                <h1 class="text-xl md:text-2xl font-bold glass-text">Cohesion</h1>
            </div>
            <a href="/logout" class="apple-button px-4 py-2 md:px-6 flex items-center space-x-2">
                <i class="fas fa-sign-out-alt text-sm md:text-base"></i>
                <span class="hidden sm:inline">Sign Out</span>
            </a>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="flex flex-col items-center justify-center responsive-container pb-8 md:pb-12">
        
        <!-- Welcome Section -->
        <div class="welcome-glass p-6 md:p-8 mb-8 md:mb-12 text-center fade-in-up relative z-10 w-full max-w-4xl">
            <div class="float-animation mb-4 md:mb-6">
                <div class="w-16 h-16 md:w-20 md:h-20 mx-auto mb-3 md:mb-4 icon-container flex items-center justify-center">
                    <i class="fas fa-user-graduate text-white text-2xl md:text-3xl"></i>
                </div>
            </div>
            <h1 class="text-2xl md:text-4xl lg:text-5xl font-bold glass-text mb-3 md:mb-4">
                Welcome back, <%= username %>!
            </h1>
            <p class="subtitle-text text-base md:text-lg lg:text-xl max-w-2xl mx-auto leading-relaxed">
                Continue your learning journey with personalized quizzes and track your progress
            </p>
        </div>

        <!-- Dashboard Cards -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 md:gap-6 lg:gap-8 w-full max-w-7xl">

            <!-- Questions Card -->
            <a href="/questions" class="liquid-glass-card p-6 md:p-8 text-center group fade-in-up delay-1">
                <div class="icon-container w-12 h-12 md:w-16 md:h-16 mx-auto mb-4 md:mb-6 flex items-center justify-center group-hover:scale-110 transition-transform duration-300">
                    <i class="fas fa-question-circle text-white text-xl md:text-2xl"></i>
                </div>
                <h3 class="text-lg md:text-xl font-bold glass-text mb-2 md:mb-3">Questions</h3>
                <p class="subtitle-text text-xs md:text-sm leading-relaxed mb-4 md:mb-0">
                    Test your knowledge with interactive quizzes and challenging exercises
                </p>
                <div class="mt-4 md:mt-6 flex items-center justify-center text-blue-600">
                    <span class="text-xs md:text-sm font-medium">Start Quiz</span>
                    <i class="fas fa-arrow-right ml-2 group-hover:translate-x-1 transition-transform text-xs md:text-sm"></i>
                </div>
            </a>

            <!-- Progress Card -->
            <a href="/progress" class="liquid-glass-card p-6 md:p-8 text-center group fade-in-up delay-2">
                <div class="icon-container green w-12 h-12 md:w-16 md:h-16 mx-auto mb-4 md:mb-6 flex items-center justify-center group-hover:scale-110 transition-transform duration-300">
                    <i class="fas fa-chart-line text-white text-xl md:text-2xl"></i>
                </div>
                <h3 class="text-lg md:text-xl font-bold glass-text mb-2 md:mb-3">Progress</h3>
                <p class="subtitle-text text-xs md:text-sm leading-relaxed mb-4 md:mb-0">
                    Track your learning journey and monitor your improvement over time
                </p>
                <div class="mt-4 md:mt-6 flex items-center justify-center text-emerald-600">
                    <span class="text-xs md:text-sm font-medium">View Progress</span>
                    <i class="fas fa-arrow-right ml-2 group-hover:translate-x-1 transition-transform text-xs md:text-sm"></i>
                </div>
            </a>

            <!-- Results Card -->
            <a href="/my-results" class="liquid-glass-card p-6 md:p-8 text-center group fade-in-up delay-3">
                <div class="icon-container purple w-12 h-12 md:w-16 md:h-16 mx-auto mb-4 md:mb-6 flex items-center justify-center group-hover:scale-110 transition-transform duration-300">
                    <i class="fas fa-trophy text-white text-xl md:text-2xl"></i>
                </div>
                <h3 class="text-lg md:text-xl font-bold glass-text mb-2 md:mb-3">Results</h3>
                <p class="subtitle-text text-xs md:text-sm leading-relaxed mb-4 md:mb-0">
                    Review your quiz results and analyze your performance metrics
                </p>
                <div class="mt-4 md:mt-6 flex items-center justify-center text-purple-600">
                    <span class="text-xs md:text-sm font-medium">View Results</span>
                    <i class="fas fa-arrow-right ml-2 group-hover:translate-x-1 transition-transform text-xs md:text-sm"></i>
                </div>
            </a>

            <!-- Profile Card -->
            <a href="/profile" class="liquid-glass-card p-6 md:p-8 text-center group fade-in-up delay-4">
                <div class="icon-container orange w-12 h-12 md:w-16 md:h-16 mx-auto mb-4 md:mb-6 flex items-center justify-center group-hover:scale-110 transition-transform duration-300">
                    <i class="fas fa-user-cog text-white text-xl md:text-2xl"></i>
                </div>
                <h3 class="text-lg md:text-xl font-bold glass-text mb-2 md:mb-3">Profile</h3>
                <p class="subtitle-text text-xs md:text-sm leading-relaxed mb-4 md:mb-0">
                    Manage your account settings and personalize your experience
                </p>
                <div class="mt-4 md:mt-6 flex items-center justify-center text-amber-600">
                    <span class="text-xs md:text-sm font-medium">Edit Profile</span>
                    <i class="fas fa-arrow-right ml-2 group-hover:translate-x-1 transition-transform text-xs md:text-sm"></i>
                </div>
            </a>

        </div>

        <!-- Quick Stats -->
        <div class="welcome-glass p-4 md:p-6 mt-8 md:mt-12 w-full max-w-4xl fade-in-up delay-4">
            <h2 class="text-xl md:text-2xl font-bold glass-text mb-4 md:mb-6 text-center">Quick Stats</h2>
            <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 md:gap-6">
                <div class="text-center p-3 md:p-0">
                    <div class="text-2xl md:text-3xl font-bold glass-text">124</div>
                    <div class="subtitle-text text-xs md:text-sm">Questions Answered</div>
                </div>
                <div class="text-center p-3 md:p-0">
                    <div class="text-2xl md:text-3xl font-bold glass-text">87%</div>
                    <div class="subtitle-text text-xs md:text-sm">Average Score</div>
                </div>
                <div class="text-center p-3 md:p-0">
                    <div class="text-2xl md:text-3xl font-bold glass-text">15</div>
                    <div class="subtitle-text text-xs md:text-sm">Days Streak</div>
                </div>
            </div>
        </div>

    </div>

</body>
</html>
