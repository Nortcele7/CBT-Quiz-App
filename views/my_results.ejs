<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>My Past Results</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen p-6">
  <div class="max-w-6xl mx-auto">
    <h1 class="text-4xl font-bold text-gray-800 mb-6">Past Results for <%= username %></h1>

    <% if(tests.length === 0) { %>
      <p class="text-gray-600 text-lg">No results yet!</p>
    <% } else { %>
      <div class="overflow-x-auto">
        <table class="min-w-full bg-white shadow-lg rounded-lg overflow-hidden">
          <thead class="bg-blue-600 text-white">
            <tr>
              <th class="px-6 py-3 text-left">Test ID</th>
              <th class="px-6 py-3 text-left">Date</th>
              <th class="px-6 py-3 text-left">Total Points</th>
              <th class="px-6 py-3 text-left">Total Possible</th>
              <th class="px-6 py-3 text-left">Score (%)</th>
              <th class="px-6 py-3 text-left">Action</th>
            </tr>
          </thead>
          <tbody>
            <% tests.forEach(test => { %>
              <tr class="border-b">
                <td class="px-6 py-4 font-medium"><%= test.testId %></td>
                <td class="px-6 py-4"><%= new Date(test.createdAt).toLocaleString() %></td>
                <td class="px-6 py-4"><%= test.totalPoints %></td>
                <td class="px-6 py-4"><%= test.totalPossible %></td>
                <td class="px-6 py-4"><%= ((test.totalPoints / test.totalPossible) * 100).toFixed(2) %>%</td>
                <td class="px-6 py-4">
                  <a href="/results/<%= test.testId %>" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition">View Detailed</a>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } %>
  </div>
</body>
</html>
