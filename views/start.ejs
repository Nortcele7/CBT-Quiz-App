<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cohesion - Start</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        :root {
            --sky-blue: #87CEEB;
            --light-green: #90EE90;
            --light-yellow: #FFD700;
        }

        /* Full viewport video overlay */
        #intro {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background: black;
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 50;
        }

        #intro video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* Hide main content until video ends */
        #main-content {
            display: none;
        }
    </style>
</head>
<body class="bg-white font-sans overflow-hidden">

    <!-- Full-page video -->
    <div id="intro">
        <video id="intro-video" autoplay muted>
            <source src="/images/cohesion_logo_intro.mp4" type="video/mp4">
            Your browser does not support the video tag.
        </video>
    </div>

    <!-- Main content -->
    <div id="main-content" class="min-h-screen flex flex-col items-center justify-center p-6 space-y-6">
        <img src="/images/cohesion_logo.png" alt="Cohesion Logo" class="w-48 h-auto">
        <h1 class="text-4xl md:text-5xl font-extrabold text-gray-900 text-center">Welcome to Cohesion</h1>
        <p class="text-lg md:text-xl text-gray-800 max-w-2xl text-center">
            Practice tests, track progress, and make studying easier. Cohesion makes learning smooth and fun!
        </p>

        <!-- Feature cards -->
        <div class="flex flex-col md:flex-row gap-4 w-full max-w-5xl justify-center">
            <div class="flex-1 bg-white border-4 border-[var(--sky-blue)] rounded-2xl p-6 shadow-lg text-center hover:scale-105 transform transition">
                <h2 class="text-2xl font-bold mb-2">Practice Tests</h2>
                <p class="text-base">Take interactive tests and evaluate yourself regularly.</p>
            </div>
            <div class="flex-1 bg-white border-4 border-[var(--light-green)] rounded-2xl p-6 shadow-lg text-center hover:scale-105 transform transition">
                <h2 class="text-2xl font-bold mb-2">Track Progress</h2>
                <p class="text-base">Monitor your learning journey and improve continuously.</p>
            </div>
            <div class="flex-1 bg-white border-4 border-[var(--light-yellow)] rounded-2xl p-6 shadow-lg text-center hover:scale-105 transform transition">
                <h2 class="text-2xl font-bold mb-2">Study Made Easier</h2>
                <p class="text-base">Organized content and interactive features simplify your preparation.</p>
            </div>
        </div>

        <!-- Navigation -->
        <div class="space-x-6 mt-6">
            <a href="/register" class="border-4 border-[var(--light-green)] px-6 py-2 rounded-full font-semibold text-gray-900 hover:bg-[var(--light-green)] hover:text-white transition text-lg">Register</a>
            <a href="/login" class="border-4 border-[var(--light-yellow)] px-6 py-2 rounded-full font-semibold text-gray-900 hover:bg-[var(--light-yellow)] hover:text-white transition text-lg">Login</a>
        </div>
    </div>

    <script>
        const video = document.getElementById("intro-video");
        const intro = document.getElementById("intro");
        const mainContent = document.getElementById("main-content");

        video.addEventListener("ended", () => {
            intro.style.display = "none";
            mainContent.style.display = "flex";
        });
    </script>

</body>
</html>
